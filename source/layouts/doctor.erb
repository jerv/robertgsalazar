---
title: Robert G. Salazar M.D.
---

<div class="title">
	<div class="container">
		<h1>Robert G. Salazar, M.D.</h1>
	</div>
</div>

<div class="container">
	<div class="row padding-xl scroll-on-page">
		
		<%= yield %>
		
	</div>
</div>

<div class="u-additional">
	<h2>Additional Profiles &amp; Information</h2>
	<div class="row">
		<div class="col-xs-12 col-sm-6">
		  <% link_to 'johncox.html' do %>
		    <h5>William J. Johncox</h5>
		    <p>D.C., P.A.-C</p>
		  <% end %>
		</div>
		<div class="col-xs-12 col-sm-6">
		  <% link_to 'our-facilities.html' do %>
		    <h5>Our Facilities</h5>
		    <p>About our Location</p>
		  <% end %>
		</div>
	</div>
</div>

<script type="text/javascript">
	(function (jQuery) {
	  jQuery.mark = {
	    jump: function (options) {
	      var defaults = {
	        selector: 'a.scroll-on-page-link'
	      };
	      if (typeof options == 'string') {
	        defaults.selector = options;
	      }

	      options = jQuery.extend(defaults, options);
	      return jQuery(options.selector).click(function (e) {
	        var jumpobj = jQuery(this);
	        var target = jumpobj.attr('href');
	        var thespeed = 1000;
	        var offset = jQuery(target).offset().top;
	        jQuery('html,body').animate({
	          scrollTop: offset
	        }, thespeed, 'swing');
	        e.preventDefault();
	      });
	    }
	  };
	})(jQuery);


	jQuery(function(){  
	  jQuery.mark.jump();
	});
</script>